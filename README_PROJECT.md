# Repositório Monorepo — Snapshot

Estrutura esperada do repositório (monorepo):

```
/backend
/docs
/docs/adr
/docs/AGENT_CONTEXT
/docs/sprints
/docs/epics
/docs/epics/histories
/frontend
/scripts
README.md
```

Breve instruções:
- Docs centrais em `docs/`.
- Backend: código em `backend/src/`; testes em `backend/src/tests`.
- Frontend: código em `frontend/`.

Atualize `docs/DOCS_INDEX.md` quando mover ou adicionar documentação importante.
