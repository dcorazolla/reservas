meta {
  name: set-invoice-id
  type: http
  seq: 1
}

get {
  url: {{api_url}}/api/invoices
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearer_token}}
}

script:post-response {
  let data = res.body.data;
  bru.setVar('invoice_id', data[0].id);
}
