meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{api_url}}/api/room-categories
  body: json
  auth: bearer
}

headers {
  X-Property-Id: {{property_id}}
}

auth:bearer {
  token: {{bearer_token}}
}

body:json {
  {
    "name": "Suíte",
    "description": "Categoria de teste"
  }
}
