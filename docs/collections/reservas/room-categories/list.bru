meta {
  name: list
  type: http
  seq: 1
}

get {
  url: {{api_url}}/api/room-categories
  body: none
  auth: bearer
}

headers {
  X-Property-Id: {{property_id}}
}

auth:bearer {
  token: {{bearer_token}}
}
