meta {
  name: 200-get-all
  type: http
  seq: 1
}

get {
  url: {{api_url}}/api/rooms
  body: none
  auth: bearer
}

headers {
  X-Property-Id: {{property_id}}
}

auth:bearer {
  token: {{bearer_token}}
}
