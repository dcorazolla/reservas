meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{api_url}}/api/rooms
  body: json
  auth: bearer
}

headers {
  X-Property-Id: {{property_id}}
}

auth:bearer {
  token: {{bearer_token}}
}

body:json {
  {
    "name": "Quarto 101",
    "number": "101",
    "capacity": 2,
    "room_category_id": "{{category_id}}"
  }
}
